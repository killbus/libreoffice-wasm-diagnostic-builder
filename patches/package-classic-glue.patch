diff --git a/RepositoryFixes.mk b/RepositoryFixes.mk
--- a/RepositoryFixes.mk
+++ b/RepositoryFixes.mk
@@ -38,7 +38,7 @@ ifeq ($(OS),EMSCRIPTEN)
 ifeq ($(ENABLE_QT6),TRUE)
 gb_Executable_FILENAMES := $(patsubst soffice_bin:soffice_bin%,soffice_bin:soffice.js,$(gb_Executable_FILENAMES))
 else
-gb_Executable_FILENAMES := $(patsubst soffice_bin:soffice_bin%,soffice_bin:soffice.mjs,$(gb_Executable_FILENAMES))
+gb_Executable_FILENAMES := $(patsubst soffice_bin:soffice_bin%,soffice_bin:soffice.js,$(gb_Executable_FILENAMES))
 endif
 else
 gb_Executable_FILENAMES := $(patsubst soffice_bin:soffice_bin%,soffice_bin:soffice.bin,$(gb_Executable_FILENAMES))
diff --git a/desktop/Executable_soffice_bin.mk b/desktop/Executable_soffice_bin.mk
--- a/desktop/Executable_soffice_bin.mk
+++ b/desktop/Executable_soffice_bin.mk
@@ -67,14 +67,3 @@ $(eval $(call gb_Executable_add_ldflags,soffice_bin,\
-# MODULARIZE wraps the Emscripten output in a factory function, allowing
-# proper async initialization and multiple instantiations if needed.
-# EXPORT_ES6 outputs ES6 module format with import/export syntax.
-# EXPORT_NAME defines the name for the default export factory function.
-$(eval $(call gb_Executable_add_ldflags,soffice_bin, \
-    -s MODULARIZE=1 \
-    -s EXPORT_ES6=1 \
-    -s EXPORT_NAME=createSofficeModule \
-    --minify 0 \
-))
-
 $(call gb_Executable_get_linktarget_target,soffice_bin): \
     $(gb_CustomTarget_workdir)/static/unoembind/bindings_uno.js \
     $(EMSCRIPTEN_EXTRA_SOFFICE_POST_JS)
diff --git a/solenv/gbuild/platform/unxgcc.mk b/solenv/gbuild/platform/unxgcc.mk
--- a/solenv/gbuild/platform/unxgcc.mk
+++ b/solenv/gbuild/platform/unxgcc.mk
@@ -174 +174 @@
-	$(if $(filter EMSCRIPTEN,$(OS)),$(if $(filter TRUE,$(HAVE_EXTERNAL_DWARF)),&& emdwp -e $(patsubst %$(gb_Executable_EXT),%.wasm,$(1)) -o $(patsubst %$(gb_Executable_EXT),%.wasm.dwp,$(1)))) \
+	$(if $(filter EMSCRIPTEN,$(OS)),$(if $(filter TRUE,$(HAVE_EXTERNAL_DWARF)),&& emdwp -e $(basename $(1)).wasm -o $(basename $(1)).wasm.dwp)) \
